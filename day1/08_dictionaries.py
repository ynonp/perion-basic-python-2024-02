"""
Create dictionaries with { ... }
numbers = { 'one': 1, 'two': 2, 'three': 3 }

Use functions you already know:
len(numbers) - number of keys
'one' in numbers - is 'one' a valid key

for key, value in numbers.items():
    print(key, value)

numbers['five'] => get item (raising exception if key not found)
numbers.get('five', 'default') => get item for key 'five' or 'default'
"""

